<form action="/users" method="get">
    <input type="search" required name="term" value="<?= htmlspecialchars($term['term'])?>">
    <input type="submit" value="Search">
</form>

<ul>
    <?php foreach ($users as $user) :?>
        <li><?= $user ?></li>
    <?php endforeach; ?>
</ul>